<Window x:Class="epicture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:epicture"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" WindowStartupLocation="CenterScreen" WindowState="Maximized"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <DataTemplate x:Key="listBoxTemplate">
            <StackPanel Margin="3">
                <DockPanel>
                    <Image Source="{Binding Url}" Height="150" />
                </DockPanel>
                <DockPanel>
                    <Button Content="Ajouter aux favories" Click="Button_Click" />
                </DockPanel>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>

        <TextBox Name ="searching" Height="23" TextWrapping="Wrap" Text="" Margin="10,10,50,100" VerticalAlignment="Top"/>

        <Button Content="Go to Favorite" Click="Display_Favoris" Name="Fav"  Margin="40"  HorizontalAlignment="left" VerticalAlignment="Top" Height="45">
            <Button.Template>
                <ControlTemplate>
                    <Image Source="Icons/favoris.png" Height="37" Margin="31,0,0,0" RenderTransformOrigin="-0.528,0.372"/>
                </ControlTemplate>
            </Button.Template>
        </Button>


        <Button Content="OK"  Click="onTextChangedSearch" Margin="10"   HorizontalAlignment="Right" VerticalAlignment="Top" Height="23" RenderTransformOrigin="-0.125,0.386">
            <Button.Template>
                <ControlTemplate>
                    <Image Source="Icons/tick1.png"/>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <ComboBox Name="type"  Height="30" Width="121" VerticalAlignment="Top"   HorizontalAlignment="Right" Margin="0,47,10,0" >
            <ComboBoxItem IsSelected="True">all types</ComboBoxItem>
            <ComboBoxItem>Jpg</ComboBoxItem>
            <ComboBoxItem>Png</ComboBoxItem>
            <ComboBoxItem>Gif</ComboBoxItem>
            <ComboBoxItem>Anigif</ComboBoxItem>
        </ComboBox>
        <ComboBox Name="format"  Height="30" Width="121" VerticalAlignment="Top"   HorizontalAlignment="Right" Margin="0,47,173,0" >
            <ComboBoxItem IsSelected="True">all size</ComboBoxItem>
            <ComboBoxItem>Small</ComboBoxItem>
            <ComboBoxItem>Mediuù</ComboBoxItem>
            <ComboBoxItem>Big</ComboBoxItem>
            <ComboBoxItem>Large</ComboBoxItem>
            <ComboBoxItem>Hudge</ComboBoxItem>
        </ComboBox>
        <ListBox Name="listBox1" ItemsSource="{Binding Contents}" ItemTemplate="{StaticResource listBoxTemplate}" Margin="10,90,10,10"/>


    </Grid>
</Window>

<!-- Window x:Class="epicture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:epicture"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Window.Resources>
        <DataTemplate x:Key="DisplayImageTemplate">
            <StackPanel Margin="3">
                <DockPanel>
                    <Image Source="{Binding contents}" Height="150" />
                    </DockPanel>
            </StackPanel>
                </DataTemplate>
    </Window.Resources>
    <Grid>
        <TextBox Grid.Row="1"  Name="MyTextBox" Margin="10" VerticalAlignment="Top"/>
        <Button Grid.Row="2" Content="OK" Click="Button_Click" Margin="40" VerticalAlignment="Top"/>
        <ListBox Grid.Row="3" Margin="100" Name="DisplayImage1" ItemsSource="{Binding Contents}" ItemTemplate="{StaticResource DisplayImageTemplate}" SelectionChanged="listBox1_SelectionChanged"/>
    </Grid>
</>
    
-->