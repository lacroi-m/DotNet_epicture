<Window x:Class="epicture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:epicture"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" WindowStartupLocation="CenterScreen" WindowState="Maximized"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <DataTemplate x:Key="listBoxTemplate">
            <StackPanel Margin="3">
                <DockPanel>
                    <Image Source="{Binding Url}" Height="150" />
                </DockPanel>
                <DockPanel>
                    <Button Content="Ajouter aux favories" Click="Button_Click" />
                </DockPanel>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        
        <TextBox Name ="searching" Height="23" TextWrapping="Wrap" Text="" Margin="10,10,10,0" VerticalAlignment="Top"/>
        <Button Grid.Row="2" Content="OK"  Click="onTextChangedSearch" Margin="40" Width="90" VerticalAlignment="Top"/>

        <ComboBox Name="type"  Height="30" Width="121" VerticalAlignment="Top"   HorizontalAlignment="Right" Margin="0,40,0,0" >


            <ComboBoxItem IsSelected="True">all</ComboBoxItem>
            <ComboBoxItem>Jpg</ComboBoxItem>
            <ComboBoxItem>Png</ComboBoxItem>
            <ComboBoxItem>Gif</ComboBoxItem>
            <ComboBoxItem>Anigif</ComboBoxItem>


        </ComboBox>
            <ListBox Name="listBox1" ItemsSource="{Binding Contents}" ItemTemplate="{StaticResource listBoxTemplate}" Margin="10,90,10,10"/>


    </Grid>
</Window>

<!-- Window x:Class="epicture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:epicture"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Window.Resources>
        <DataTemplate x:Key="DisplayImageTemplate">
            <StackPanel Margin="3">
                <DockPanel>
                    <Image Source="{Binding contents}" Height="150" />
                    </DockPanel>
            </StackPanel>
                </DataTemplate>
    </Window.Resources>
    <Grid>
        <TextBox Grid.Row="1"  Name="MyTextBox" Margin="10" VerticalAlignment="Top"/>
        <Button Grid.Row="2" Content="OK" Click="Button_Click" Margin="40" VerticalAlignment="Top"/>
        <ListBox Grid.Row="3" Margin="100" Name="DisplayImage1" ItemsSource="{Binding Contents}" ItemTemplate="{StaticResource DisplayImageTemplate}" SelectionChanged="listBox1_SelectionChanged"/>
    </Grid>
</>
    
-->